sylius_fixtures:
    suites:
        default:
            fixtures:
                promotion:
                    options:
                        custom:
                            referrals_program:
                                code: 'referrals_program'
                                name: 'Referrals program'
                                channels:
                                    - 'FASHION_WEB'
                                coupon_based: true
