<button
    class="ui secondary icon labeled button"
    type="button"
    data-share-link-btn
    data-href="{{ path('odiseo_sylius_referrals_plugin_shop_partial_referrals_program_create_from_product', { 'id': product.id }) }}"
    data-csrf="{{ csrf_token('referrals_program_' ~ product.slug) }}">
        <i class="alternate share icon"></i>
        {{ 'odiseo_sylius_referrals_plugin.ui.share_link'|trans }}
</button>
